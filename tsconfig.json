{
  // 根 tsconfig 主要负责把配置拆分给浏览器端和 Node 侧两个子配置文件。
  "files": [],
  "references": [
    // 浏览器端 React 应用配置。
    { "path": "./tsconfig.app.json" },
    // Node / Vite 配置文件所需的 TypeScript 配置。
    { "path": "./tsconfig.node.json" }
  ]
}
